{
  "mcpServers": {
    "wokwi": {
      "type": "stdio",
      "command": "wokwi-cli",
      "args": [
        "mcp"
      ],
      "env": {
        "WOKWI_CLI_TOKEN": "${WOKWI_CLI_TOKEN}"
      }
    }
  }
}